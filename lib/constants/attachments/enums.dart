/// Enum to store all the media types that can be uploaded in the app
enum MediaType{
  image, video, websiteCard
}

/// Enum to store all the sources of the media types
enum MediaSourceType {
  network, file
}